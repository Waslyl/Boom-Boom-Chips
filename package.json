{
  "name": "boom-boom-chips",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build:shared": "npm run build -w @bbc/shared",
    "dev": "npm run build:shared && concurrently -n shared,server,client -c magenta,cyan,green \"npm run dev -w @bbc/shared\" \"npm run dev -w @bbc/server\" \"npm run dev -w @bbc/client\"",
    "build": "npm run build:shared && npm run build -w @bbc/server && npm run build -w @bbc/client",
    "start": "node server/dist/index.js",
    "test": "npm run build:shared && vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "npm run build",
    "lint": "npm run typecheck",
    "start:e2e": "node scripts/start-e2e.mjs",
    "test:all": "npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.10.2",
    "concurrently": "^9.1.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
